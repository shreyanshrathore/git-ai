// import { api } from "~/trpc/react";
// import { useLocalStorage } from "usehooks-ts";
// const useProject = () => {
//   const { data: projects } = api.user.createChat.useMutation();
//   const [chatId, setChatId] = useLocalStorage("chat", "");
//   const project = projects?.find((project) => project.id === projectId);
//   return {
//     projects,
//     project,
//     projectId,
//     setProjectId,
//   };
// };

// export default useProject;
